const { create } = require('./create')
const { deal } = require('./deal')
const { shuffle } = require('./shuffle')
const { shuffleOnDeck } = require('./shuffleOnDeck')

module.exports = {
  create,
  deal,
  shuffle,
  shuffleOnDeck
}
